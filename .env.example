# Spotify Developer App credentials
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=

# Only needed for user auth (write/replace). Must match your app settings.
SPOTIFY_REDIRECT_URI=http://localhost:8888/callback
