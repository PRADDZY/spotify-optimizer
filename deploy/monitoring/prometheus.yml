# Prometheus config (single-node VPS)

global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "spotify-optimizer-api"
    metrics_path: /metrics
    scheme: https
    static_configs:
      - targets: ["api.example.com"]
