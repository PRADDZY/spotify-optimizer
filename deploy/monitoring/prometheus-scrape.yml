# Prometheus scrape config snippet
# Local scrape example for single-node VPS.

scrape_configs:
  - job_name: "spotify-optimizer-api"
    metrics_path: /metrics
    scheme: http
    static_configs:
      - targets: ["127.0.0.1:8000"]
